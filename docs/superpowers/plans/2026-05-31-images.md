# 이미지 (붙여넣기/드롭 + 로컬 저장 + 렌더) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 이미지를 붙여넣기/드롭하면 문서 옆 `assets/` 폴더에 저장되고 `![](assets/…)` 링크가 삽입되며, 그 링크가 에디터에서 이미지로 렌더된다.

**Architecture:** Rust 코어가 새 `save_asset` 커맨드로 바이트를 받아 `<문서폴더>/assets/<해시>.<확장>`에 원자적 저장하고 상대경로를 반환(신뢰 경계 유지). 프론트는 CM6 paste/drop 핸들러로 바이트를 캡처해 IPC 호출 후 링크 삽입, 그리고 Image 노드를 `convertFileSrc`로 만든 `<img>` 위젯으로 렌더. Tauri 에셋 프로토콜을 켜서 웹뷰가 로컬 파일을 로드.

**Tech Stack:** Rust(`sha2`), Tauri 2 asset protocol, CodeMirror 6, `@tauri-apps/api`(`convertFileSrc`), Vitest.

> 결정: 이미지 저장 = **문서 옆 `assets/`**(문서에 path가 있어야 함; 미저장 문서면 먼저 저장 요구). 렌더 = 에셋 프로토콜 + `convertFileSrc`. 보안 스코프는 우선 넉넉히 잡고(로컬 에디터) M3에서 열린 폴더 기준으로 좁힘.
>
> **시각 검증:** 붙여넣기/드롭과 이미지 렌더는 사람이 `npm run tauri dev`로 확인·반복.

---

## 파일 구조
```
src-tauri/src/
├─ fs_ops.rs       # MODIFY: save_asset() + write_bytes_atomic() + 테스트
├─ commands.rs     # MODIFY: save_asset 커맨드
└─ Cargo.toml      # MODIFY: sha2
src/
├─ ipc/bindings.ts # MODIFY: saveAsset 추가
├─ editor/
│  ├─ docContext.ts  # NEW: 현재 문서 디렉토리 공유(get/set)
│  ├─ paste.ts       # NEW: paste/drop → save_asset → 링크 삽입 (domEventHandlers)
│  └─ image.ts       # NEW: Image 노드 → <img> 위젯 (convertFileSrc)
│  └─ editor.ts      # MODIFY: paste/image 확장 연결
└─ main.ts          # MODIFY: 문서 로드/저장 시 docContext 갱신
src-tauri/tauri.conf.json + capabilities/default.json  # MODIFY: asset protocol
```

---

## Task 1: Rust `save_asset` (TDD)

**Files:** Modify `src-tauri/src/fs_ops.rs`, `src-tauri/Cargo.toml`

- [ ] **Step 1: sha2 추가** `cd src-tauri && cargo add sha2 && cd ..`

- [ ] **Step 2: 실패 테스트** — `fs_ops.rs`의 `#[cfg(test)] mod tests`에 추가:
```rust
    #[test]
    fn save_asset_writes_hashed_file_and_returns_relative_path() {
        let dir = tempfile::tempdir().unwrap();
        let bytes = b"\x89PNG fake image";
        let rel = save_asset(dir.path(), bytes, "png").unwrap();
        assert!(rel.starts_with("assets/") && rel.ends_with(".png"));
        assert!(dir.path().join(&rel).exists());
    }

    #[test]
    fn save_asset_is_idempotent_for_same_bytes() {
        let dir = tempfile::tempdir().unwrap();
        let b = b"same";
        let a1 = save_asset(dir.path(), b, "png").unwrap();
        let a2 = save_asset(dir.path(), b, "png").unwrap();
        assert_eq!(a1, a2);
    }
```

- [ ] **Step 3: cargo test fs_ops → 컴파일 실패 확인**

- [ ] **Step 4: 구현** — `fs_ops.rs`에 추가(`use sha2::{Digest, Sha256};`를 상단 use에):
```rust
fn write_bytes_atomic(path: &Path, bytes: &[u8]) -> Result<(), String> {
    let tmp = temp_sibling(path)?;
    {
        let mut f = fs::File::create(&tmp).map_err(|e| format!("create temp failed '{}': {e}", tmp.display()))?;
        f.write_all(bytes).map_err(|e| format!("write temp failed '{}': {e}", tmp.display()))?;
        f.sync_all().map_err(|e| format!("sync failed '{}': {e}", tmp.display()))?;
    }
    fs::rename(&tmp, path).map_err(|e| format!("rename failed: {e}"))
}

/// 바이트를 <doc_dir>/assets/<sha256>.<ext> 에 저장하고 "assets/<name>" 반환.
pub fn save_asset(doc_dir: &Path, bytes: &[u8], ext: &str) -> Result<String, String> {
    let assets = doc_dir.join("assets");
    fs::create_dir_all(&assets).map_err(|e| format!("create assets dir failed: {e}"))?;
    let mut h = Sha256::new();
    h.update(bytes);
    let hash = format!("{:x}", h.finalize());
    let safe_ext = ext.chars().filter(|c| c.is_ascii_alphanumeric()).collect::<String>();
    let name = if safe_ext.is_empty() { hash } else { format!("{hash}.{safe_ext}") };
    let path = assets.join(&name);
    if !path.exists() { write_bytes_atomic(&path, bytes)?; }
    Ok(format!("assets/{name}"))
}
```
(`write_text_file_atomic`은 그대로 두고, 텍스트 경로는 그걸 계속 사용.)

- [ ] **Step 5: cargo test fs_ops → 통과(텍스트 5 + 이미지 2 = 7)**

- [ ] **Step 6: 커밋** `git add src-tauri/src/fs_ops.rs src-tauri/Cargo.toml src-tauri/Cargo.lock` ; `git commit -m "feat(img): save_asset — content-hashed image storage with tests"`

---

## Task 2: IPC 커맨드 + 에셋 프로토콜

**Files:** Modify `src-tauri/src/commands.rs`, `src/ipc/bindings.ts`, `src-tauri/tauri.conf.json`, `src-tauri/capabilities/default.json`

- [ ] **Step 1: 커맨드** — `commands.rs`에 추가:
```rust
use std::path::Path;

#[tauri::command]
pub fn save_asset(doc_path: String, bytes: Vec<u8>, ext: String) -> Result<String, String> {
    let dir = Path::new(&doc_path).parent().ok_or("문서 경로에 폴더가 없음")?;
    fs_ops::save_asset(dir, &bytes, &ext)
}
```
그리고 `lib.rs`의 `generate_handler![...]`에 `commands::save_asset` 추가.

- [ ] **Step 2: 바인딩** — `src/ipc/bindings.ts`의 `commands` 객체에 추가:
```ts
  saveAsset: (docPath: string, bytes: number[], ext: string) =>
    call<string>("save_asset", { docPath, bytes, ext }),
```

- [ ] **Step 3: 에셋 프로토콜 켜기** — `src-tauri/tauri.conf.json`의 `app.security`에 추가(없으면 생성):
```json
"assetProtocol": { "enable": true, "scope": ["**"] }
```
(우선 넉넉히 `**`; M3에서 열린 폴더 기준으로 좁힌다.) 그리고 `capabilities/default.json`의 `permissions`에 `"core:asset:default"` 추가.

- [ ] **Step 4: 검증** `cd src-tauri ; cargo check ; cd ..`(에셋 스코프/권한 JSON 검증) ; `npx tsc --noEmit` ; `npm run build`.

- [ ] **Step 5: 커밋** `git add src-tauri/src/commands.rs src-tauri/src/lib.rs src/ipc/bindings.ts src-tauri/tauri.conf.json src-tauri/capabilities/default.json` ; `git commit -m "feat(img): save_asset IPC command + asset protocol"`

---

## Task 3: 붙여넣기/드롭 → 저장 → 링크 삽입

**Files:** Create `src/editor/docContext.ts`, `src/editor/paste.ts`; Modify `src/editor/editor.ts`, `src/main.ts`

- [ ] **Step 1: 문서 디렉토리 공유.** Create `src/editor/docContext.ts`:
```ts
let docPath: string | null = null;
export function setDocPath(p: string | null) { docPath = p; }
export function getDocPath(): string | null { return docPath; }
export function getDocDir(): string | null {
  if (!docPath) return null;
  const i = Math.max(docPath.lastIndexOf("/"), docPath.lastIndexOf("\\"));
  return i >= 0 ? docPath.slice(0, i) : null;
}
```

- [ ] **Step 2: paste/drop 핸들러.** Create `src/editor/paste.ts`:
```ts
import { EditorView } from "@codemirror/view";
import { commands } from "../ipc/bindings";
import { getDocPath } from "./docContext";

function extFromType(type: string): string {
  const m = /image\/(\w+)/.exec(type);
  return m ? (m[1] === "jpeg" ? "jpg" : m[1]) : "png";
}

async function handleFile(view: EditorView, file: File) {
  const docPath = getDocPath();
  if (!docPath) { alert("이미지를 넣으려면 먼저 문서를 저장하세요 (Ctrl/Cmd-S)."); return; }
  const bytes = Array.from(new Uint8Array(await file.arrayBuffer()));
  const res = await commands.saveAsset(docPath, bytes, extFromType(file.type));
  if (res.status === "error") { console.error(res.error); return; }
  const pos = view.state.selection.main.head;
  view.dispatch({ changes: { from: pos, insert: `![](${res.data})` }, selection: { anchor: pos + 2 } });
}

export const imagePaste = EditorView.domEventHandlers({
  paste(e, view) {
    const items = e.clipboardData?.items;
    if (!items) return false;
    for (const it of items) {
      if (it.kind === "file" && it.type.startsWith("image/")) {
        const f = it.getAsFile();
        if (f) { e.preventDefault(); void handleFile(view, f); return true; }
      }
    }
    return false;
  },
  drop(e, view) {
    const files = e.dataTransfer?.files;
    if (!files || files.length === 0) return false;
    const imgs = Array.from(files).filter((f) => f.type.startsWith("image/"));
    if (imgs.length === 0) return false;
    e.preventDefault();
    for (const f of imgs) void handleFile(view, f);
    return true;
  },
});
```

- [ ] **Step 3: 연결.** `editor.ts`: `import { imagePaste } from "./paste";` → extensions에 `imagePaste,` 추가. `main.ts`: `import { setDocPath } from "./editor/docContext";` 그리고 `loadedDoc`/`markSaved` 직후(문서 path 확정 시) `setDocPath(docState.path)` 호출. 초기 1회도.

- [ ] **Step 4: 검증** `npx tsc --noEmit` ; `npm run build`.

- [ ] **Step 5: 사람 스모크** — 문서를 먼저 저장한 뒤 이미지를 붙여넣기/드롭 → `assets/`에 파일 생기고 `![](assets/…)` 삽입되는지, 미저장 시 안내 뜨는지. (아직 렌더는 Task 4 — 링크 텍스트로만 보임.)

- [ ] **Step 6: 커밋** `git add src/editor/docContext.ts src/editor/paste.ts src/editor/editor.ts src/main.ts` ; `git commit -m "feat(img): paste/drop image -> save_asset -> insert link"`

---

## Task 4: 이미지 렌더 위젯

**Files:** Create `src/editor/image.ts`; Modify `src/editor/editor.ts`

- [ ] **Step 1: 이미지 위젯.** Create `src/editor/image.ts` — Lezer `Image` 노드(`![alt](url)`)를 찾아 `<img>`로 replace(커서 진입 시 원본). 상대경로는 `getDocDir()`로 절대화 후 `convertFileSrc`:
```ts
import { syntaxTree } from "@codemirror/language";
import { type EditorState, type Extension, RangeSetBuilder, StateField } from "@codemirror/state";
import { Decoration, type DecorationSet, EditorView, WidgetType } from "@codemirror/view";
import { convertFileSrc } from "@tauri-apps/api/core";
import { getDocDir } from "./docContext";

function urlFromImageNode(state: EditorState, from: number, to: number): string | null {
  const text = state.doc.sliceString(from, to);
  const m = /\]\(([^)]+)\)/.exec(text); // ![alt](url)
  return m ? m[1].trim() : null;
}

function resolveSrc(url: string): string {
  if (/^(https?:|data:|asset:)/.test(url)) return url;
  const dir = getDocDir();
  if (!dir) return url;
  const sep = dir.includes("\\") ? "\\" : "/";
  return convertFileSrc(dir + sep + url.replace(/\//g, sep));
}

class ImageWidget extends WidgetType {
  constructor(readonly src: string, readonly alt: string) { super(); }
  eq(o: ImageWidget) { return o.src === this.src && o.alt === this.alt; }
  toDOM() {
    const img = document.createElement("img");
    img.className = "cm-md-image";
    img.src = this.src;
    img.alt = this.alt;
    return img;
  }
  ignoreEvent() { return false; }
}

function cursorInside(state: EditorState, from: number, to: number): boolean {
  for (const r of state.selection.ranges) if (r.to >= from && r.from <= to) return true;
  return false;
}

function build(state: EditorState): DecorationSet {
  const b = new RangeSetBuilder<Decoration>();
  syntaxTree(state).iterate({
    enter: (node) => {
      if (node.name !== "Image") return;
      if (cursorInside(state, node.from, node.to)) return;
      const url = urlFromImageNode(state, node.from, node.to);
      if (!url) return;
      const altM = /!\[([^\]]*)\]/.exec(state.doc.sliceString(node.from, node.to));
      b.add(node.from, node.to, Decoration.replace({ widget: new ImageWidget(resolveSrc(url), altM?.[1] ?? "") }));
    },
  });
  return b.finish();
}

export function imagePreview(): Extension {
  return StateField.define<DecorationSet>({
    create: (s) => build(s),
    update: (d, tr) => (tr.docChanged || tr.selection ? build(tr.state) : d),
    provide: (f) => [EditorView.decorations.from(f), EditorView.atomicRanges.from(f)],
  });
}
```
> 메모: `EditorView.atomicRanges.from(f)` 타입 이슈가 나면 `EditorView.atomicRanges.of((view) => view.state.field(f))`로(Task M2 수식과 동일 패턴). 또한 문서 path가 바뀌면 위젯 src가 갱신돼야 하므로, `setDocPath` 변경 시 에디터에 빈 트랜잭션을 디스패치해 재빌드하거나, M3에서 docDir을 Facet으로 승격.

- [ ] **Step 2: 연결 + 스타일.** `editor.ts`: `import { imagePreview } from "./image";` → extensions에 `imagePreview(),`. `editorTheme.ts`: `".cm-md-image": { maxWidth: "100%", borderRadius: "8px", display: "block", margin: "0.4em 0" }`.

- [ ] **Step 3: 검증** `npx tsc --noEmit` ; `npm run build`.

- [ ] **Step 4: 사람 스모크** — Task 3로 붙여넣은 이미지가 **렌더**되는지, 커서 진입 시 원본 `![](…)`로, 외부 URL 이미지(`![](https://…)`)도 뜨는지. (안 뜨면 `convertFileSrc` URL/경로 구분자/에셋 스코프를 스모크로 보정.)

- [ ] **Step 5: 커밋** `git add src/editor/image.ts src/editor/editor.ts src/theme/editorTheme.ts` ; `git commit -m "feat(img): inline image rendering via asset protocol"`

---

## Task 5: 통합 검증 + 마무리
- [ ] 전체 테스트(프론트 9 + Rust 7) ; 릴리스 빌드.
- [ ] 사람 종합 스모크: 붙여넣기→저장→렌더, 미저장 안내, 외부 URL, M2 회귀.
- [ ] README 현황 갱신(이미지 추가).
- [ ] 커밋 → `main` 병합·푸시.

## 알려진 한계 / 다음
- 에셋 스코프 `**`(넓음) → M3에서 열린 폴더 기준으로 좁힘.
- 문서 path 변경 시 이미지 위젯 src 갱신은 단순 처리 → M3에서 docDir Facet으로 정리.
- 드롭 좌표→커서 위치 매핑은 단순(현재 선택 위치 삽입) → 추후 개선.
- 큰 이미지 리사이즈/지연 로딩 = 폴리시.
